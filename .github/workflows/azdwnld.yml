on:
  schedule:
    - cron: '30 * * * *'
  workflow_dispatch:

permissions: write-all

defaults:
  run:
    shell: bash
    working-directory: ./

jobs:
  configurator:
    runs-on: ubuntu-latest
    steps:
    - name: Download antizapret pac file
      run: |
        ls -ral ./
        seq 1 10 >./data/new.txt
        #wget -O data/proxy.pac -- 'https://antizapret.prostovpn.org/proxy.pac'
